import java.io.*;
import java.util.*;

public class InputProcessor {

  /*  public static void main(String[] args) throws IOException {
    FileOutputStream p = null; FileInputStream b = null; FileOutputStream p1 = null;
    try{
        LiquidStream water = new LiquidStream("water",1000.0,21.0, new double[] {33.46,0.6880,0.7604,-3.593});
        System.out.println("Printing data to water.txt..."); p = new FileOutputStream("water.txt"); water.writeData(p);
        System.out.println("Reading data from input.txt..."); b = new FileInputStream("input.txt"); water.readData(b);
        System.out.println("Printing data to output.txt..."); p1 = new FileOutputStream("output.txt"); water.writeData(p1);
    } finally{
    //if (p != null) p.close(); //because closing the stream in the finally block can throw an IOException, we must have our method declare itself to throw rthis, rather than use a catch block
    //if (b != null) b.close();
    //if (p1 != null) p1.close(); }
    //} }
*/
}
