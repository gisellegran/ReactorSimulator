import java.util.Scanner;

public class Test {/*

    public static void main(String[] args) {
        int n=2;
        double[] inletFlowRates = new double[n];
        System.out.println("Enter the inlet flowrates of the species in the following order:  separated by a comma.");
        Scanner scan3 = new Scanner(System.in);
        String flowrates = scan3.nextLine();
        Scanner scan4 = new Scanner(flowrates);
        scan4.useDelimiter(",");
        for (int i = 0; i < n; i++) {
            if (scan4.hasNextInt()) {
                inletFlowRates[i] = (double)scan4.nextInt();
            }
            inletFlowRates[i] = scan4.nextDouble();
        }
    }
*/}
